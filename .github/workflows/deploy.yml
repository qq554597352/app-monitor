name: 构建并部署到 GitHub Pages

on:
  push:
    branches: [main]
    paths:
      - 'client/**'
      - 'data/**'
      - '.github/workflows/deploy.yml'
  workflow_run:
    workflows: ["爬取APP数据并更新"]
    types: [completed]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: 检出仓库
        uses: actions/checkout@v4

      - name: 设置 Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: client/package-lock.json

      - name: 安装前端依赖
        working-directory: client
        run: npm ci --legacy-peer-deps

      - name: 构建前端
        working-directory: client
        run: npm run build
        env:
          VITE_BASE_URL: /app-monitor/

      - name: 复制数据文件到构建产物
        run: |
          mkdir -p client/dist/data/history
          cp -r data/* client/dist/data/ 2>/dev/null || echo "No data files yet"

      - name: 上传 Pages 产物
        uses: actions/upload-pages-artifact@v3
        with:
          path: client/dist

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: 部署到 GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
